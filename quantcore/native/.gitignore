/target
# Compiled extension dropped in-tree by `maturin develop` (editable install).
# Machine/Python-specific; regenerated by the build — never committed.
/python/quantcore_native/*.so
/python/quantcore_native/*.pyd
/python/quantcore_native/*.dylib

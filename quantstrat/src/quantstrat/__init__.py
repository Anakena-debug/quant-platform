"""quantstrat — research backtesting layer.

Composes quantcore primitives into Strategy subclasses and walk-forward backtests;
produces performance artefacts (tearsheets, metrics). Does not fit models (that's
quantcore) or execute orders (that's quantengine).
"""

__version__ = "0.1.0"
